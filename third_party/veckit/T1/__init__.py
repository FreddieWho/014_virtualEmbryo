"""Task 1 baselines and data loading (see TASK1.md). Not meant to be imported as a package directly --
score_h5ad.py/run_v2.py add this directory to sys.path and import its modules (data, metrics, ...) by
their plain names, since T1/T2/T3 share module names by design (one task per process). This file exists
so setuptools recognizes T1/ as an installable package.
"""
