# T3 路线六首次执行（2026-09-21）

实际完成来源准入检查、20个训练基因/6个留出基因评估、26个来源基因最终训练和Gata4目标推断。状态 **FAILED_DISASTER**，两个输出均未通过负值比例门槛，候选数0，未提交/未评分；selection保持。blocks_submission: false（本实验输出失败，不是全任务合规禁令）。

## 执行与证据

用户授权“路线六开始”。按 `reports/t3_r56_launch_20260921/START.md` 原命令执行，未更改输入、100步训练设计、随机种子或1%门槛。

- 运行目录：`artifacts/t3_next/T3-R6-OP2-20260921-v1/`；RESULT.json记录核心执行3.66秒，退出码0表示程序正常记录失败，不代表候选通过。
- `INPUT_LOCK.json`保留输入与代码哈希，code/保留实现及设计快照；本报告的DIAGNOSTICS.json补充来源manifest、最终权重、父版本及结果哈希。
- 父版本v0009；候选ID/artifact/contract：无/未生成/NOT_RUN。没有新zip，也没有修改既有候选或权威分数登记。
- graph_holdout.pt和graph_final.pt均已保存。诊断脚本仅加载最终图模型权重推断，按原参数重拟合确定性的最终Ridge以补记负值数量；未重训GCN、换种子或调参。输入哈希复核通过。

## 留出评估通过

| 对照 | 留出MSE |
|---|---:|
| 不预测变化 | 0.0056773401 |
| Ridge | 0.0045728264 |
| 图模型 | 0.0041094352 |

图模型比Ridge低10.13%，比不预测变化低27.62%。这是固定划分下成人心脏成纤维细胞的整基因留出结果，没有独立重复或显著性结论；不等于Gata4胚胎预测准确，也不是服务器分数提升。

## 输出失败

| 输出 | 负值个数 / 全部表达条目 | 负值比例 | 允许上限 |
|---|---:|---:|---:|
| r6ridge | 1251805 / 3724500 | 33.6100% | 1% |
| r6graph | 1442141 / 3724500 | 38.7204% | 1% |

当前方法给所有目标细胞加同一条signed变化向量。加法不保证表达非负；大量条目需截零，按既定门槛停止生成候选。不能把截零视为无损处理，也不能据此说学习器完全无效。跨成人→胚胎的泛化仍NOT_VALIDATED；两个样本共用NTC均值，批次混杂仍未排除。全panel匹配Gata4真值评分NOT_RUN_NO_MATCHED_GATA4_TARGET。

建议下一版先解决表达输出的非负约束（如表达量依赖的乘性变化），再做独立来源/样本敏感性检查；需要新设计和新run，保留本轮失败记录，不自动放宽门槛。本次未执行修复分支。R5 v0032/v0033仍未提交/未评分，下次提交前需回填已有候选分数和原始证据。
